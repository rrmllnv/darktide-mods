# Объяснение: Можно ли использовать другие реплики из игры?

## Краткий ответ

**Да, можно!** Но есть ограничения и нюансы.

## Типы голосовых реплик в игре

### 1. Реплики для колеса коммуникации (`on_demand_com_wheel`)

Это реплики, которые **специально созданы** для колеса коммуникации. Они определены в:
- `scripts/settings/dialogue/vo_query_constants.lua` 
- Файлы: `dialogues/generated/on_demand_vo_*.lua`

**Их можно использовать напрямую** в колесе, указав:
```lua
voice_event_data = {
    voice_tag_concept = "on_demand_com_wheel",
    voice_tag_id = "com_wheel_vo_xxx",  -- Из vo_query_constants.lua
}
```

**Доступные реплики:**
- `com_wheel_vo_for_the_emperor` - "За Императора!"
- `com_wheel_vo_yes` - "Да"
- `com_wheel_vo_no` - "Нет"
- `com_wheel_vo_thank_you` - "Спасибо"
- `com_wheel_vo_follow_you` - "Следую за тобой"
- `com_wheel_vo_my_pleasure` - "Пожалуйста"
- `com_wheel_vo_need_that` - "Мне это нужно" (dibs)
- `com_wheel_vo_take_this` - "Возьми это"
- И другие (см. `ALL_VOICE_OPTIONS.md`)

### 2. Реплики из gameplay_vo (контекстные реплики)

Это реплики, которые персонажи говорят **автоматически** в разных ситуациях:
- `calling_for_help` - когда просят помощи
- `almost_there` - "Почти на месте"
- `away_from_squad` - "Отстал от отряда"
- `combat_pause_one_liner` - короткие фразы в бою
- `ability_maniac` - при использовании способности
- И многие другие...

**Где они находятся:**
- Файлы: `dialogues/generated/gameplay_vo_*.lua`
- Используются с концептом `generic_mission_vo` или `interaction_vo`

**Можно ли их использовать в колесе?**

**Технически ДА**, но нужно использовать другой концепт:

```lua
voice_event_data = {
    voice_tag_concept = "generic_mission_vo",  -- Вместо on_demand_com_wheel
    voice_tag_id = "calling_for_help",  -- Имя из gameplay_vo файла
}
```

**Проблемы:**
1. Эти реплики **не предназначены** для колеса - они контекстные
2. Могут звучать странно вне контекста
3. Нужно знать точное имя реплики из gameplay_vo файла
4. Не все реплики подходят для ручного вызова

### 3. Реплики для тегов предметов (`on_demand_vo_tag_item`)

Используются при тегировании предметов:
- `pup_ammo` - "Патроны здесь"
- `pup_medical_crate` - "Аптечка здесь"
- И другие (см. `vo_query_constants.lua`)

**Можно использовать:**
```lua
voice_event_data = {
    voice_tag_concept = "on_demand_vo_tag_item",
    voice_tag_id = "pup_ammo",
}
```

## Как найти подходящие реплики

### Шаг 1: Откройте файл gameplay_vo

Например: `Darktide-Source-Code/dialogues/generated/gameplay_vo_zealot_male_c.lua`

### Шаг 2: Найдите интересующие реплики

Ищите по ключевым словам:
- `calling_for_help` - просьба о помощи
- `almost_there` - "почти на месте"
- `away_from_squad` - "отстал от отряда"
- `combat_pause_one_liner` - короткие фразы
- `ability_*` - способности
- `alerted_*` - предупреждения
- `found_*` - находки

### Шаг 3: Проверьте структуру

Каждая реплика имеет структуру:
```lua
calling_for_help = {
    randomize_indexes_n = 0,
    sound_events_n = 6,
    sound_events = {
        "loc_zealot_male_c__calling_for_help_01",
        "loc_zealot_male_c__calling_for_help_02",
        -- ...
    },
    -- ...
}
```

**Имя реплики** = `calling_for_help` (это trigger_id)

### Шаг 4: Используйте в колесе

```lua
[WHEEL_OPTION.help_custom] = {
    display_name = "loc_help_custom",
    icon = "content/ui/materials/hud/interactions/icons/help",
    voice_event_data = {
        voice_tag_concept = "generic_mission_vo",  -- Важно!
        voice_tag_id = "calling_for_help",  -- Имя из gameplay_vo
    },
}
```

## Примеры использования реплик из gameplay_vo

### Пример 1: "Почти на месте"

```lua
[WHEEL_OPTION.almost_there] = {
    display_name = "loc_almost_there",
    icon = "content/ui/materials/hud/communication_wheel/icons/location",
    voice_event_data = {
        voice_tag_concept = "generic_mission_vo",
        voice_tag_id = "almost_there",  -- Из gameplay_vo
    },
}
```

### Пример 2: "Отстал от отряда"

```lua
[WHEEL_OPTION.away_from_squad] = {
    display_name = "loc_away_from_squad",
    icon = "content/ui/materials/hud/communication_wheel/icons/attention",
    voice_event_data = {
        voice_tag_concept = "generic_mission_vo",
        voice_tag_id = "away_from_squad",  -- Из gameplay_vo
    },
}
```

### Пример 3: Короткие фразы в бою

```lua
[WHEEL_OPTION.combat_phrase] = {
    display_name = "loc_combat_phrase",
    icon = "content/ui/materials/hud/communication_wheel/icons/enemy",
    voice_event_data = {
        voice_tag_concept = "generic_mission_vo",
        voice_tag_id = "combat_pause_one_liner",  -- Из gameplay_vo
    },
}
```

## Важные замечания

### ✅ Что работает хорошо:

1. **Реплики из on_demand_com_wheel** - созданы специально для колеса
2. **Реплики из on_demand_vo_tag_item** - для предметов
3. **Некоторые контекстные реплики** - если они подходят по смыслу

### ⚠️ Что может быть проблемой:

1. **Контекстные реплики** могут звучать странно вне контекста
2. **Нужно знать точное имя** реплики из gameplay_vo файла
3. **Не все реплики подходят** для ручного вызова
4. **Разные голоса** могут иметь разные наборы реплик

### 🔍 Как проверить, есть ли реплика:

1. Откройте файл `gameplay_vo_*_*.lua` для нужного голоса
2. Найдите имя реплики (например, `calling_for_help`)
3. Проверьте, что она есть во всех нужных голосах
4. Используйте с концептом `generic_mission_vo`

## Рекомендации

1. **Начните с реплик из `on_demand_com_wheel`** - они точно работают
2. **Для экспериментов** используйте `generic_mission_vo` с репликами из gameplay_vo
3. **Тестируйте** - не все реплики звучат хорошо вне контекста
4. **Проверяйте локализацию** - нужны ключи для display_name

## Итог

**Да, можно использовать другие реплики!** 

- **Для колеса** - используйте `on_demand_com_wheel` (14 доступных реплик)
- **Для экспериментов** - используйте `generic_mission_vo` с репликами из gameplay_vo
- **Для предметов** - используйте `on_demand_vo_tag_item`

Главное - найти подходящие реплики и правильно указать концепт и trigger_id.

