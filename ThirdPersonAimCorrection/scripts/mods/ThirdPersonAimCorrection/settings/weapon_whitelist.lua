return {
	-- Set to true to apply correction to every weapon that uses ActionShoot.
	all_weapons = true,
	templates = {
		-- Add weapon_action_component.template_name values here after enabling debug logging.
		-- Example:
		-- your_weapon_template_name = true,
	},
	items = {
		-- Add item.name or item.__master_item values here if the template name is not specific enough.
		-- Example:
		-- your_item_name = true,
	},
}
