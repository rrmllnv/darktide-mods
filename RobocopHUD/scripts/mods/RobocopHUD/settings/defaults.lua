local defaults = {
	robocophud_enabled = true,
	theme_id = "classic_green",
	hud_opacity = 1.0,
	hud_scale = 1.0,

	targeting_enabled = true,
	target_hold_seconds = 1.0,
	lock_scan_seconds = 0.20,
	lock_track_seconds = 0.25,

	hide_vanilla_stamina = false,
	hide_vanilla_dodge = false,

	debug_overlay = false,
}

return defaults

